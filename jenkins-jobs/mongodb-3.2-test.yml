# Job definition to test MongoDB connector against MongoDB 3.2
---
- job:
    project-type: freestyle
    name: debezium-mongodb-3.2-test
    display-name: Debezium MongoDB 3.2 Test
    description: Executes tests for MongoDB Connector with MongoDB 3.2
    node: Slave
    properties:
      - build-discarder:
          days-to-keep: 7
      - github:
          url: https://github.com/debezium/debezium
    parameters:
      - string:
          name: REPOSITORY
          default: https://github.com/debezium/debezium
          description: "Repository from which Debezium is built"
      - string:
          name: BRANCH
          default: master
          description: "A branch from which Debezium is built"
    scm:
      - git:
         url: $REPOSITORY
         branches:
          - "*/$BRANCH"
    wrappers:
      - timeout:
          timeout: 45
    triggers:
      - timed: "H 05 * * 1-5"
    builders:
      - maven-target:
          maven-version: (Default)
          goals: >-
          clean install -U -s $HOME/.m2/settings-snapshots.xml
          -pl debezium-connector-mongodb -am -fae
          -Dmaven.test.failure.ignore=true -Dversion.mongo.server=3.2
    publishers:
      - junit:
          results: "**/target/surefire-reports/*.xml,**/target/failsafe-reports/*.xml"
      - email:
          recipients: jpechane@redhat.com

DECLARE
    TYPE T2 IS RECORD (C NUMBER);
    TYPE T1 IS RECORD (B T2);
    A T1;

    TYPE T3 IS TABLE OF NUMBER INDEX BY BINARY_INTEGER;
    D T3;
BEGIN
    SELECT 1
    INTO A.B.C
    FROM DUAL;

    SELECT 1
    INTO D(1)
    FROM DUAL;

    SELECT 1
    INTO :BLOCK.ITEM
    FROM DUAL;
END;
/
